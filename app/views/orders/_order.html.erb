<tr>
  <td><%= order.id %></td>
  <td ><%= order.grade %></td>
  <td><%= order.notes %></td>
  <td><%= order.updated_at.strftime("%B %e, %Y") %></td>
  <td><%= order.created_at.strftime("%B %e, %Y") %></td>
</tr>
